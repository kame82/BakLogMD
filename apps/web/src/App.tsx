import { APP_NAME } from '@baklogmd/shared';

export function App() {
  return (
    <main className="container">
      <h1>{APP_NAME} Web</h1>
      <p>OAuth対応のWebフロントエンド雛形です。</p>
    </main>
  );
}
